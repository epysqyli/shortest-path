mod graph;

// build only two paths with two layers to begin with
// A -> B -> D where A->B = 3, B->D = 4
// A -> C -> D where A->C = 6, C->D = 2
// the best path would of course in this case be A -> B -> D with a total cost of 7 instead of 8
// create the node struct 
fn main() {

}
